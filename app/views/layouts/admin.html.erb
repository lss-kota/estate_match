<!DOCTYPE html>
<html>
  <head>
    <title>EstateMatch - 管理画面</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 font-sans antialiased">
    <!-- 管理者ナビゲーション -->
    <nav class="bg-gray-800 shadow-lg border-b border-gray-700">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex justify-between items-center h-16">
          <!-- ロゴ・タイトル -->
          <div class="flex items-center">
            <%= link_to admin_root_path, class: "flex items-center" do %>
              <div class="bg-gray-700 text-red-400 font-bold text-xl px-3 py-2 rounded-lg mr-3 border border-red-400">
                EM
              </div>
              <span class="text-white text-xl font-bold">管理画面</span>
            <% end %>
          </div>

          <!-- ナビゲーションメニュー -->
          <div class="hidden md:flex items-center space-x-4">
            <%= link_to "ダッシュボード", admin_dashboard_path, 
                  class: "text-gray-300 hover:text-white hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
            <%= link_to "ユーザー管理", admin_users_path, 
                  class: "text-gray-300 hover:text-white hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
            <%= link_to "物件管理", admin_properties_path, 
                  class: "text-gray-300 hover:text-white hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
            <%= link_to "メッセージ", "#", 
                  class: "text-gray-300 hover:text-white hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
          </div>

          <!-- ユーザー情報・ログアウト -->
          <div class="flex items-center space-x-4">
            <span class="text-gray-300 text-sm">
              <%= current_user.name %>（管理者）
            </span>
            <%= link_to "サイトに戻る", root_path, 
                  class: "text-gray-300 hover:text-white hover:bg-gray-700 px-3 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
            <%= link_to "ログアウト", destroy_user_session_path, data: { "turbo-method": :delete },
                  class: "bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium transition-colors duration-200" %>
          </div>
        </div>
      </div>
    </nav>
    
    <!-- フラッシュメッセージ -->
    <% if notice %>
      <div class="bg-green-900 border border-green-700 text-green-200 px-4 py-3 rounded mb-4 mx-4 mt-4">
        <%= notice %>
      </div>
    <% end %>
    
    <% if alert %>
      <div class="bg-red-900 border border-red-700 text-red-200 px-4 py-3 rounded mb-4 mx-4 mt-4">
        <%= alert %>
      </div>
    <% end %>
    
    <!-- メインコンテンツ -->
    <main class="max-w-7xl mx-auto py-6 px-4 sm:px-6 lg:px-8">
      <%= yield %>
    </main>
  </body>
</html>