<!-- ユーザー管理 - 編集 -->
<div class="space-y-6">
  <!-- ページヘッダー -->
  <div class="bg-gray-800 shadow-lg rounded-lg border border-gray-700 p-6">
    <div class="flex items-center justify-between">
      <div>
        <h1 class="text-2xl font-bold text-white">ユーザー編集</h1>
        <p class="text-gray-300 mt-1">ユーザー「<%= @user.name %>」の情報を編集します</p>
      </div>
      <div class="flex space-x-3">
        <%= link_to "詳細に戻る", admin_user_path(@user),
              class: "px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500" %>
        <%= link_to "一覧に戻る", admin_users_path,
              class: "px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500" %>
      </div>
    </div>
  </div>

  <!-- エラーメッセージ -->
  <% if @user.errors.any? %>
    <div class="bg-red-50 border border-red-200 rounded-md p-4">
      <div class="flex">
        <div class="flex-shrink-0">
          <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
            <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
          </svg>
        </div>
        <div class="ml-3">
          <h3 class="text-sm font-medium text-red-800">
            <%= pluralize(@user.errors.count, "個のエラー") %>が発生しました
          </h3>
          <div class="mt-2 text-sm text-red-700">
            <ul class="list-disc list-inside space-y-1">
              <% @user.errors.full_messages.each do |message| %>
                <li><%= message %></li>
              <% end %>
            </ul>
          </div>
        </div>
      </div>
    </div>
  <% end %>

  <!-- 編集フォーム -->
  <div class="bg-gray-800 shadow-lg rounded-lg border border-gray-700 p-6">
    <%= form_with model: [:admin, @user], local: true, class: "space-y-6" do |form| %>
      <!-- 基本情報 -->
      <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div>
          <%= form.label :name, "ユーザー名", class: "block text-sm font-medium text-gray-200 mb-1" %>
          <%= form.text_field :name, 
                required: true,
                class: "w-full px-3 py-2 border border-gray-600 bg-gray-700 text-white placeholder-gray-400 rounded-md focus:outline-none focus:ring-red-500 focus:border-red-500 #{'border-red-500' if @user.errors[:name].any?}" %>
          <% if @user.errors[:name].any? %>
            <p class="mt-1 text-sm text-red-600"><%= @user.errors[:name].first %></p>
          <% end %>
        </div>

        <div>
          <%= form.label :email, "メールアドレス", class: "block text-sm font-medium text-gray-200 mb-1" %>
          <%= form.email_field :email, 
                required: true,
                class: "w-full px-3 py-2 border border-gray-600 bg-gray-700 text-white placeholder-gray-400 rounded-md focus:outline-none focus:ring-red-500 focus:border-red-500 #{'border-red-500' if @user.errors[:email].any?}" %>
          <% if @user.errors[:email].any? %>
            <p class="mt-1 text-sm text-red-600"><%= @user.errors[:email].first %></p>
          <% end %>
        </div>
      </div>

      <!-- ユーザータイプ -->
      <div>
        <%= form.label :user_type, "ユーザータイプ", class: "block text-sm font-medium text-gray-200 mb-3" %>
        <div class="space-y-3">
          <% [['buyer', '購入者'], ['owner', 'オーナー'], ['admin', '管理者']].each do |value, label| %>
            <label class="flex items-center">
              <%= form.radio_button :user_type, value, 
                    class: "h-4 w-4 text-red-600 focus:ring-red-500 border-gray-600 bg-gray-700 text-white placeholder-gray-400" %>
              <span class="ml-3">
                <span class="text-sm font-medium text-white"><%= label %></span>
                <span class="text-sm text-gray-400 block">
                  <%= case value
                      when 'buyer' then '物件を購入したいユーザー'
                      when 'owner' then '物件を売りたいオーナー'
                      when 'admin' then '管理者権限を持つユーザー'
                      end %>
                </span>
              </span>
            </label>
          <% end %>
        </div>
        <% if @user.errors[:user_type].any? %>
          <p class="mt-1 text-sm text-red-600"><%= @user.errors[:user_type].first %></p>
        <% end %>
      </div>

      <!-- 登録情報（編集不可） -->
      <div class="border-t border-gray-600 pt-6">
        <h3 class="text-lg font-medium text-white mb-4">登録情報</h3>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div>
            <label class="block text-sm font-medium text-gray-200 mb-1">登録日時</label>
            <div class="px-3 py-2 bg-gray-700 border border-gray-600 rounded-md text-sm text-white">
              <%= @user.created_at.strftime('%Y年%m月%d日 %H:%M') %>
              （<%= time_ago_in_words(@user.created_at) %>前）
            </div>
          </div>

          <div>
            <label class="block text-sm font-medium text-gray-200 mb-1">最終更新</label>
            <div class="px-3 py-2 bg-gray-700 border border-gray-600 rounded-md text-sm text-white">
              <%= @user.updated_at.strftime('%Y年%m月%d日 %H:%M') %>
              （<%= time_ago_in_words(@user.updated_at) %>前）
            </div>
          </div>
        </div>
      </div>

      <!-- アクティビティ統計（編集不可） -->
      <div class="border-t border-gray-600 pt-6">
        <h3 class="text-lg font-medium text-white mb-4">アクティビティ統計</h3>
        <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
          <div class="text-center p-4 bg-gray-700 rounded-lg">
            <div class="text-2xl font-bold text-blue-600"><%= @user.properties.count %></div>
            <div class="text-sm text-gray-300">投稿物件</div>
          </div>
          <div class="text-center p-4 bg-gray-700 rounded-lg">
            <div class="text-2xl font-bold text-green-600"><%= @user.conversations.count %></div>
            <div class="text-sm text-gray-300">会話数</div>
          </div>
          <div class="text-center p-4 bg-gray-700 rounded-lg">
            <div class="text-2xl font-bold text-purple-600"><%= @user.sent_messages.count %></div>
            <div class="text-sm text-gray-300">送信メッセージ</div>
          </div>
          <div class="text-center p-4 bg-gray-700 rounded-lg">
            <div class="text-2xl font-bold text-yellow-600"><%= @user.favorites.count %></div>
            <div class="text-sm text-gray-300">お気に入り</div>
          </div>
        </div>
      </div>

      <!-- 注意事項 -->
      <div class="bg-yellow-50 border border-yellow-200 rounded-md p-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-yellow-800">編集時の注意事項</h3>
            <div class="mt-2 text-sm text-yellow-700">
              <ul class="list-disc list-inside space-y-1">
                <li>ユーザータイプを変更すると、アクセスできる機能が変わります</li>
                <li>管理者権限を付与する場合は、信頼できるユーザーのみにしてください</li>
                <li>メールアドレスを変更すると、ユーザーに確認メールが送信される場合があります</li>
              </ul>
            </div>
          </div>
        </div>
      </div>

      <!-- 送信ボタン -->
      <div class="flex justify-end space-x-3 pt-6 border-t border-gray-600">
        <%= link_to "キャンセル", admin_user_path(@user),
              class: "px-4 py-2 bg-gray-300 text-gray-700 rounded-md hover:bg-gray-400 focus:outline-none focus:ring-2 focus:ring-gray-500" %>
        <%= form.submit "変更を保存",
              class: "px-6 py-2 bg-red-600 text-white rounded-md hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-red-500 transition-colors duration-200" %>
      </div>
    <% end %>
  </div>
</div>