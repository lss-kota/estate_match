<!DOCTYPE html>
<html>
  <head>
    <title>管理画面アクセス認証 - EstateMatch</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="robots" content="noindex, nofollow">
    
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body class="bg-gray-900 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full space-y-8 px-4">
      <div>
        <!-- ロゴ・タイトル -->
        <div class="flex justify-center">
          <div class="bg-gray-800 text-red-400 font-bold text-2xl px-4 py-3 rounded-lg border border-red-400">
            EM
          </div>
        </div>
        <h2 class="mt-6 text-center text-3xl font-extrabold text-white">
          管理画面アクセス認証
        </h2>
        <p class="mt-2 text-center text-sm text-gray-300">
          管理画面にアクセスするには、パスワードの入力が必要です
        </p>
      </div>

      <!-- エラーメッセージ -->
      <% if flash[:alert] %>
        <div class="bg-red-900 border border-red-700 rounded-md p-4">
          <div class="flex">
            <div class="flex-shrink-0">
              <svg class="h-5 w-5 text-red-400" viewBox="0 0 20 20" fill="currentColor">
                <path fill-rule="evenodd" d="M10 18a8 8 0 100-16 8 8 0 000 16zM8.707 7.293a1 1 0 00-1.414 1.414L8.586 10l-1.293 1.293a1 1 0 101.414 1.414L10 11.414l1.293 1.293a1 1 0 001.414-1.414L11.414 10l1.293-1.293a1 1 0 00-1.414-1.414L10 8.586 8.707 7.293z" clip-rule="evenodd" />
              </svg>
            </div>
            <div class="ml-3">
              <h3 class="text-sm font-medium text-red-200">
                <%= flash[:alert] %>
              </h3>
            </div>
          </div>
        </div>
      <% end %>

      <!-- パスワード入力フォーム -->
      <%= form_with url: admin_auth_path, method: :post, local: true, class: "mt-8 space-y-6" do |form| %>
        <div>
          <%= form.label :password, "管理画面パスワード", class: "block text-sm font-medium text-gray-200" %>
          <%= form.password_field :password, 
                required: true,
                class: "mt-1 appearance-none relative block w-full px-3 py-2 border border-gray-600 placeholder-gray-400 text-white bg-gray-800 rounded-md focus:outline-none focus:ring-red-500 focus:border-red-500 focus:z-10 sm:text-sm",
                placeholder: "パスワードを入力してください" %>
        </div>

        <div>
          <button type="submit" class="group relative w-full flex justify-center py-3 px-4 border border-transparent text-sm font-medium rounded-md text-white bg-red-600 hover:bg-red-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-red-500 transition-colors duration-200">
            管理画面にアクセス
          </button>
        </div>
      <% end %>

      <!-- 通常サイトに戻るリンク -->
      <div class="text-center">
        <%= link_to "通常サイトに戻る", root_path, 
              class: "text-red-400 hover:text-red-300 text-sm font-medium" %>
      </div>
    </div>

    <script>
      // パスワードフィールドにフォーカス
      document.addEventListener('DOMContentLoaded', function() {
        document.querySelector('input[type="password"]').focus();
      });
    </script>
  </body>
</html>